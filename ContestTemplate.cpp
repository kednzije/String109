#include <iostream>
using namespace std;
using ll = long long;
constexpr ll MAXN = 1e5 + 10;
inline ll get_min(ll a, ll b) {
	return a < b ? a : b;
}
inline ll get_max(ll a, ll b) {
	return a > b ? a : b;
}

int main() {
	cout << "Hello world!" << endl;
	return 0;
}